"""Maya (OpenMaya 2.0) node layer for the pure-Python TwistSpline.

These modules import ``maya`` and only work inside Maya. The math kernel
(``twistspline.core``) stays Maya-free and is reused unchanged.
"""
